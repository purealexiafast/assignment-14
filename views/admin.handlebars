<h2>Admin Page</h2>

<div>
    {{#each posts}}

    <div>
        <h3>
            {{title}}
        </h3>

        <p>
            {{body}}
        </p>

        <p>
            This post was created by {{user.username}} on {{createdAt}}.
        </p>
    </div>

    {{/each}}

</div>